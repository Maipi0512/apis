/**
 * Capa de presentación (Controllers).
 * <p>
 * Expone los endpoints REST de la aplicación, recibe las peticiones HTTP,
 * delega la lógica de negocio en la capa de servicios ({@code service})
 * y devuelve las respuestas (usualmente DTOs) al cliente.
 */
package com.uade.ecom.controller;

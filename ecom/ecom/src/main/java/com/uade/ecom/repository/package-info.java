/**
 * Capa de persistencia (Repositories).
 * <p>
 * Responsable del acceso a datos. Encapsula la interacción con la base de
 * datos (o la fuente de datos que se defina) y es utilizada únicamente por
 * la capa de servicios ({@code service}).
 */
package com.uade.ecom.repository;
